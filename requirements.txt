# --- Required (the pipeline runs fully offline with just these) ----------
networkx>=3.0
numpy>=1.24

# --- Recommended (better Flat RAG vectoriser + the graph PNG deliverable) --
scikit-learn>=1.3      # optional: Flat RAG falls back to a pure-NumPy TF-IDF
matplotlib>=3.7        # optional: needed only to render knowledge_graph.png
pandas>=2.0            # optional: benchmark CSV falls back to stdlib csv

# --- Optional: use a REAL LLM for extraction / answer synthesis -----------
# Set GRAPHRAG_LLM=anthropic (or openai) and the matching API key.
# anthropic>=0.39
# openai>=1.0

# Note: Neo4j and NodeRAG are intentionally NOT used in this lab — we build the
# knowledge graph with NetworkX (Step 2, Choice A) as requested.
